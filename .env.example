# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/

# Optional: GitHub webhook secret (set the same value in your repo's webhook settings)
GITHUB_WEBHOOK_SECRET=

# Port for local development (default: 5000)
PORT=5000
